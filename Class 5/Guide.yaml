host: # lançar e conectar à vm
      vagrant up
      ssh vagrant@10.0.0.101

      # usar as mesmas ssh keys que o host por causa do Google Cloud Platform
      scp ~/.ssh/id_rsa.pub vagrant@10.0.0.101:~/.ssh
      scp ~/.ssh/id_rsa vagrant@10.0.0.101:~/.ssh

vm4: # testar conexão à vm do gcp
     ssh goncalo@35.228.181.17
	   exit

     # instalar o ansible
	   sudo apt install ansible

host: # copiar os ficheiros de provision para a vm
      scp playbook.yml vagrant@10.0.0.101:~ 
      scp hosts.inv vagrant@10.0.0.101:~ 
      scp hashed_password vagrant@10.0.0.101:~
      scp sshd_config vagrant@10.0.0.101:~ 

vm4: # obter informação da vm do gcp
     ansible -i hosts.inv -u goncalo all -m ping

     # executar o playbook
     ansible-playbook -i hosts.inv -u goncalo playbook.yml

     # conectar à vm do gcp com o user tester
     ssh tester@35.228.181.17

vm gcp: # Testar mudanças do playbook
        systemctl status ssh
        systemctl status openntpd

